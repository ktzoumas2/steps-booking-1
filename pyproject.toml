[project]
name = "steps-supervision"
version = "0.1.0"
description = "Appointment booking for STEPS supervision sessions"
requires-python = ">=3.13,<3.14"
dependencies = [
    "django>=5.2,<5.3",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
